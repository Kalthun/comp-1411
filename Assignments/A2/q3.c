#include <stdio.h>

typedef enum {
    REGULAR,
    FANCY
} Frame;

typedef enum {
    WHITE,
    RED,
    BLUE,
    GREEN,
} Color;

int main() {

    double length;
    double width;
    Frame frame;
    Color color;
    int crown_count;

    return 0;
}